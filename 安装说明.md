# 安装说明（适用所有 DSH 版本）

本插件是 **DSH 本地插件**，通过「Junction 链接 + profile 三处登记」注入，**不需要联网安装、不需要 npm publish**。

---

## 零、便携性与外部依赖（换电脑前先看这个）

### 结论：插件本身完全可搬运

| 场景 | 结果 | 说明 |
|---|---|---|
| **换电脑（同为 Windows）** | ✅ **可用** | 直接拷 zip 过去装即可 |
| 千问办公装在 **不同盘 / 非标准目录** | ✅ **可用** | 通用枚举 D–Z 盘找安装位置；找不到也能用内置版本 |
| 千问办公 **版本与你不同** | ✅ **可用** | 实测 `1.0.5` / `1.1.32` / `1.2.0` 均可正常签名 |
| 千问办公 **完全没装** | ⚠️ 功能受限 | 有内置签名版本，但**凭据仍需来自已登录的 App** |
| 换 **Windows 用户账户** | ⚠️ 需重新登录 | 凭据用 DPAPI 按用户加密 |
| 换 **macOS / Linux** | ❌ 不可用 | DPAPI 与 `%APPDATA%` 是 Windows 专有 |

### ⚠️ 关键：凭据**不能**拷贝

这是最容易误解的一点。凭据文件（`%APPDATA%\QwenWorkCN\auth-v2.dat`）**拷过去也用不了**：

```
auth-v2.dat  →  AES-256-GCM 加密，密钥来自
Local State  →  os_crypt.encrypted_key，该密钥用
DPAPI        →  绑定「当前 Windows 用户」，跨机器无法解密
```

**正确做法**：在**新电脑上安装千问办公并登录一次**，让它自己生成可解的凭据。
（拷过去会报 `DPAPI 解密主密钥失败`——错误提示里已说明原因和处理办法。）

### 其他外部条件

| 条件 | 是否必须 | 说明 |
|---|---|---|
| Node.js | ✅ 必须 | 22.19+ 或 24+；「一键安装.cmd」会自动查找 |
| DSH | ✅ 必须 | 任意版本（Desktop / CLI），安装器自动探测 profile |
| 网络 | ✅ 必须 | `gateway.qwenwork.cn` 可达 |
| 第三方 npm 依赖 | ❌ 不需要 | **零运行时依赖**，不联网安装 |
| 千问办公安装目录 | ⭕ 可选 | 只用于探测签名版本号，探测不到用内置值 |

---

## 一、最快路径：双击一键安装

1. 解压本包到任意目录（**路径不要含特殊字符**，如 `&`、`^`；中文路径可以）
2. **双击 `一键安装.cmd`**
3. 看到安装成功后，**完全退出 DSH 再重启**
4. 打开 **设置 → 插件**，应看到「QwenWork 连接」

脚本会自动：找 Node.js → 探测本机所有 DSH profile → 选择正确目标 → 完成接线。

> **前提**：本机已安装并**登录过**「千问办公」桌面版
> （即 `%APPDATA%\QwenWorkCN\` 下有 `auth-v2.dat` / `Local State`）。

---

## 二、支持的 DSH 形态（自动探测）

安装器会自动找到下列位置，**无需手动指定**：

| DSH 形态 | profile 位置 | 说明 |
|---|---|---|
| **DSH Desktop** | `%APPDATA%\dsh-desktop\harness\profiles\web` | 最常用；桌面版默认 |
| DSH Desktop（备用） | `%LOCALAPPDATA%\dsh-desktop\harness\profiles\web` | 部分安装方式 |
| **DSH CLI** | `~/.dsh/profiles/web` | 命令行版 |
| DSH CLI（XDG） | `~/.config/dsh/profiles/web` | 部分 Linux 习惯 |
| **自定义** | `$DSH_HOME/profiles/web` | 设置了 `DSH_HOME` 环境变量时 |

**候选 profile 名**：`web`、`desktop`。

### 探测结果怎么看

安装时会先打印：

```
检测到以下 DSH profile：
  · DSH Desktop → C:\Users\你\AppData\Roaming\dsh-desktop\harness\profiles\web（插件 19 个，cordis 就绪）
  · DSH CLI     → C:\Users\你\.dsh\profiles\web（插件 0 个，cordis 需桥接）
```

**选择规则**：
1. **已装过本插件的** profile 优先（升级场景，不会另装一份）
2. 否则选**插件最多且有 cordis 运行时**的那个（最可能是你日常在用的）

装错了就显式指定（见下）。

---

## 三、手动安装（任何版本通用）

```powershell
# 在插件目录执行
node tools/install-to-dsh.mjs
```

### 常用参数

| 参数 | 用途 |
|---|---|
| `--dry-run` | **只预览**，不写入任何文件（装前先看一遍） |
| `--profile "<profile目录>"` | 指定安装到哪个 profile |
| `--source "<插件目录>"` | 指定插件源码位置（默认脚本上级目录） |
| `--uninstall` | 卸载 |
| `--help` | 查看帮助 |

### 例子

```powershell
# 预览
node tools/install-to-dsh.mjs --dry-run

# 装到 DSH CLI
node tools/install-to-dsh.mjs --profile "$env:USERPROFILE\.dsh\profiles\web"

# 装到桌面版
node tools/install-to-dsh.mjs --profile "$env:APPDATA\dsh-desktop\harness\profiles\web"

# 卸载
node tools/install-to-dsh.mjs --uninstall
```

---

## 四、安装器做了什么（三处接线）

DSH 加载本地插件需要**同时**满足三个条件，缺一不可：

```
1. profile\node_modules\<插件名>          ← Junction 指向插件源码
2. profile\package.json
     dependencies[name]  = "file:./node_modules/<插件名>"
     dsh.profile.bundles[]  += <插件名>
     pnpm.overrides[name] = "link:./node_modules/<插件名>"
3. 插件源码上层目录的依赖桥               ← 让插件能解析 DSH 的 peer 依赖
     node_modules\@deepseek-ai      → profiles\node_modules\@deepseek-ai
     node_modules\@earendil-works   → profiles\node_modules\@earendil-works
```

**第 3 条容易被忽略**：插件的 peer 依赖（`@deepseek-ai/dsh-llm`、`@earendil-works/pi-ai` 等）由 DSH 提供，不随插件分发。插件不在 DSH 的 node_modules 树内时，Node 按目录向上查找会找不到——依赖桥就是补这条路。

> ⚠️ **不要**在 profile 的 `cordis.patch.yml` 里加 loader 条目。
> 插件的 `package.json` 有 `dsh.bundle.patch`，DSH 会**自动加载**插件自带的
> `cordis.patch.yml`。手工再加一条会报 `duplicate loader entry id: llm-qwenwork`，
> 且该错误会让 **DSH 整个启动失败**。

---

## 五、各 DSH 版本注意事项

### DSH Desktop（0.8.x 及以后）

- 插件在 **设置 → 插件** 里显示为「QwenWork 连接」
- 卡片显示：账号、积分（含进度条）、上游速度、可选模型
- **模型选择器**里三个模型带倍率标记：`高级 · x1.00 · 默认`、`标准｜Qwen3.8-Flash · x0.10 · 推荐`、`Qwen3.8-Max · x1.10 · 新`

### DSH CLI

- CLI 的 profile 默认**没有** `profiles\node_modules` 共享目录
- 安装器会自动创建依赖桥；若 CLI 自身的依赖不在 `profiles\node_modules`，
  用 `--bridge-dir` 显式指定 DSH 依赖所在目录：

```powershell
node tools/install-to-dsh.mjs --profile "$env:USERPROFILE\.dsh\profiles\web" --bridge-dir "<DSH依赖目录>"
```

### 多 profile 共存

同时装了 Desktop 与 CLI 时，**同一时间只装一个**即可（重复安装会让两份都在，
DSH 启动时可能冲突）。要在两者间切换：

```powershell
node tools/install-to-dsh.mjs --uninstall --profile "<旧的>"
node tools/install-to-dsh.mjs --profile "<新的>"
```

---

## 六、验证安装成功

```powershell
# 1) 插件模块能否加载
node -e "import('file:///<profile>/node_modules/dsh-qwen-connect/lib/index.js').then(m=>console.log('OK', m.name))"

# 2) 卡片渲染自检（无浏览器也能跑）
node tools/client-render-check.mjs

# 3) 模型显示预览
node tools/models-preview.mjs
```

重启 DSH 后，插件状态路由应返回账号信息：

```
http://127.0.0.1:<DSH端口>/plugins/dsh-qwen-connect/status
```

---

## 七、卸载

- **双击 `一键卸载.cmd`**，或
- `node tools/install-to-dsh.mjs --uninstall`

卸载只移除接线，**不删除插件源码目录**；依赖桥也会保留（可能被其他插件共用）。

---

## 八、常见问题

| 现象 | 原因与处理 |
|---|---|
| `duplicate loader entry id: llm-qwenwork` | profile 的 `cordis.patch.yml` 里有手工加的条目 → 删掉它（插件自带 patch 已够） |
| 插件列表里看不到 | 没重启 DSH；或装到了另一个 profile（用 `--profile` 重新指定） |
| `pi-ai image input requires the durable attachment service` | 附件服务不可用；插件会自动退回纯文本模式，不影响对话 |
| 卡片显示「未登录」 | 千问办公桌面版没登录过，或凭据已过期 → 打开千问办公 App 登录一次 |
| `Cannot find package '@deepseek-ai/dsh-llm'` | 依赖桥没建好 → 重新安装，或用 `--bridge-dir` 指定 DSH 依赖目录 |

更多上游限制见 `docs/已知上游边界.md`。
