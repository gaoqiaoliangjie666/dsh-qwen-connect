# t6 验证探针归档

本目录是 verifier 在 t6（独立验证阶段 B-2）中使用的临时探针，验证完成后由 captain 归档。

**归属**：verifier
**用途**：t6 契约各验收项的取证工具

## 关于扫描器误报

`t6_http.mjs` 含用户名与 refresh token 前缀字面量 —— 这是**安全检查探针的检测模式**，
用于验证 status 路由是否泄漏真实数据。**它本身就是安全措施，不是泄漏**。

后续做 PII 扫描时请排除本目录，或注意区分「检测模式」与「真实数据」。
